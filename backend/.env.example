# This file is a template for the environment variables required by the backend.
# Copy this file to .env and fill in the values.

# The API key for the Gemini language model.
# GEMINI_API_KEY=

# Specifies the search API provider to use.
# Supported values: "google", "searxng" (for self-hosted instances).
# Default: "google"
SEARCH_API_PROVIDER="google"

# The API key for your chosen search provider (e.g., Google Custom Search API key).
# Required if SEARCH_API_PROVIDER is "google".
SEARCH_API_KEY=""

# The base URL for your SearXNG instance.
# Required if SEARCH_API_PROVIDER is "searxng".
# Example: "http://localhost:8888"
SEARXNG_BASE_URL=""

# Specifies the LLM (Large Language Model) provider.
# Supported values: "google", "openai", or a custom provider name.
# Default: "google"
LLM_PROVIDER="google"

# The base URL for a custom LLM API.
# This is used if LLM_PROVIDER is set to a custom value.
LLM_API_BASE_URL=""

# The API key for your chosen LLM provider.
# Required for most providers (e.g., OpenAI).
# For Google, if GEMINI_API_KEY is set, it might be used by default if this is empty.
LLM_API_KEY=""

# The specific model name to use with the LLM provider.
# This is often optional and allows specifying a particular model variant
# (e.g., "gpt-4", "claude-2", or a custom model identifier).
# If not set, the provider's default model may be used.
LLM_MODEL_NAME=""